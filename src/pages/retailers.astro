---
import PageLayout from '@/layouts/PageLayout.astro';
import AssetDownload from '@/components/press/AssetDownload.astro';
import { RetailerSignupForm } from '@/components/forms/RetailerSignupForm';
import { FooterSignupForm } from '@/components/forms/FooterSignupForm';
import { FORM_ENDPOINT, retailerFormConfig, formConfigs } from '@/config/forms';

// Retailer spec sheet PDF — conditional: show download if exists, "Request spec sheet" if not
const specSheetUrl = import.meta.env.PUBLIC_RETAILER_SPEC_SHEET_URL ?? '';
---

<PageLayout
  title="Wholesale — Stock the Storm | Tomato Warning"
  description="Carry Tomato Warning premium salsa in your store. Five EF levels, published Scoville ranges, 40-45% retailer margins, and a mission-driven brand story for the shelf."
  canonicalPath="/retailers/"
>
  <article class="retailers-page">
    <div class="retailers-inner">

      <header class="page-header">
        <p class="eyebrow">Wholesale</p>
        <h1>Stock the Storm.</h1>
        <p class="page-lead">
          Five premium salsas rated EF-1 to EF-5, with published Scoville ranges from 500 to 500,000+ SHU. A heat rating system your customers will understand, a brand story they will remember, and a charitable mission that keeps them coming back.
        </p>
      </header>

      <div class="retailers-body">
        <div class="retailers-value">
          <h2>Why Tomato Warning</h2>
          <ul class="value-list">
            <li>
              <strong>The EF scale solves a real problem.</strong> No FDA standard exists for "mild," "medium," or "hot." Our five-level system with published Scoville ranges gives your customers a rating they can trust — and a reason to explore all five.
            </li>
            <li>
              <strong>Five SKUs, one system.</strong> EF-1 Coastal Calm (500 SHU) to EF-5 Ground Zero (500,000+ SHU). The graduated lineup drives trial, collection, and repeat visits.
            </li>
            <li>
              <strong>Florida-made, batch consistent.</strong> Premium ingredients, glass jars, Scoville-verified production. Your customers get the same heat every time.
            </li>
            <li>
              <strong>Mission-driven shelf presence.</strong> At least 10% of net profits go to named disaster relief organizations, reported annually. A brand story that gives your store something worth talking about.
            </li>
          </ul>

          <div class="spec-sheet-wrap">
            {specSheetUrl ? (
              <AssetDownload
                href={specSheetUrl}
                label="Retailer Spec Sheet"
                description="Product dimensions, case counts, pricing tiers, and placement guidance"
                fileType="PDF"
              />
            ) : (
              <div class="spec-request">
                <p class="spec-request-text">
                  Spec sheet available on request — submit the form and we'll send it directly.
                </p>
              </div>
            )}
          </div>
        </div>

        <div class="retailers-form">
          <h2>Wholesale Inquiry</h2>
          <p class="form-intro">
            Tell us about your store. We'll follow up with pricing, minimums, and placement details.
          </p>
          <RetailerSignupForm
            client:visible
            formEndpoint={FORM_ENDPOINT}
            listId={retailerFormConfig.listId}
            analyticsEvent="retailer_signup"
            successMessage={retailerFormConfig.successMessage}
            alreadySubscribedMessage={retailerFormConfig.alreadySubscribedMessage}
            noscriptFallbackUrl={retailerFormConfig.noscriptFallbackUrl}
            sourceContext="retailers"
            privacyPolicyUrl="/privacy/"
          />
        </div>
      </div>

    </div>
  </article>

  <FooterSignupForm
    slot="footer-form"
    client:visible
    formEndpoint={FORM_ENDPOINT}
    consumerListId={formConfigs.consumer.listId}
    retailerListId={formConfigs.retailer.listId}
    nonprofitListId={formConfigs.nonprofit.listId}
    successMessage={formConfigs.consumer.successMessage}
    alreadySubscribedMessage={formConfigs.consumer.alreadySubscribedMessage}
    noscriptFallbackUrl={formConfigs.consumer.noscriptFallbackUrl}
    privacyPolicyUrl="/privacy/"
  />
</PageLayout>

<style>
  .retailers-page {
    padding: 4rem 1.5rem;
  }

  .retailers-inner {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 4rem;
  }

  .page-header {
    max-width: 720px;
  }

  .eyebrow {
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--radar-teal);
    margin: 0 0 0.75rem;
  }

  .page-header h1 {
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 800;
    color: var(--storm-charcoal);
    margin: 0 0 1rem;
    letter-spacing: -0.02em;
  }

  .page-lead {
    font-size: 1.125rem;
    line-height: 1.7;
    color: var(--storm-charcoal);
    opacity: 0.8;
    margin: 0;
    max-width: 60ch;
  }

  .retailers-body {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: start;
  }

  .retailers-value h2,
  .retailers-form h2 {
    font-size: 1.375rem;
    font-weight: 700;
    color: var(--storm-charcoal);
    margin: 0 0 1rem;
  }

  .value-list {
    list-style: none;
    padding: 0;
    margin: 0 0 2rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .value-list li {
    padding-left: 1.25rem;
    position: relative;
    font-size: 0.9375rem;
    line-height: 1.65;
    color: var(--storm-charcoal);
  }

  .value-list li::before {
    content: '→';
    position: absolute;
    left: 0;
    color: var(--radar-teal);
    font-weight: 700;
  }

  .spec-request {
    border: 1px dashed rgba(44, 44, 44, 0.2);
    border-radius: 8px;
    padding: 1rem 1.25rem;
  }

  .spec-request-text {
    font-size: 0.875rem;
    color: var(--storm-charcoal);
    opacity: 0.65;
    margin: 0;
    font-style: italic;
  }

  .form-intro {
    font-size: 0.9375rem;
    color: var(--storm-charcoal);
    opacity: 0.7;
    margin: 0 0 1.5rem;
    line-height: 1.6;
  }

  @media (max-width: 900px) {
    .retailers-body {
      grid-template-columns: 1fr;
      gap: 3rem;
    }
  }
</style>
